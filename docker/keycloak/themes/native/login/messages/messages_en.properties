# Native copy overrides for the login page (English). Only the keys the design touches —
# everything else inherits keycloak.v2's bundle.
loginAccountTitle=Welcome back
usernameOrEmail=Email or username
doLogIn=Sign in
