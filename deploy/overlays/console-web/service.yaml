apiVersion: v1
kind: Service
metadata:
  name: console-web
  labels:
    app.kubernetes.io/name: console-web
    app.kubernetes.io/part-of: native
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: console-web
  ports:
    - name: http
      port: 80
      targetPort: http
