// Runtime configuration placeholder.
//
// In production this file is REGENERATED at container start by docker-entrypoint.d/40-native-env.sh
// from the container's env vars, so a single built image works across environments. In local dev it
// stays empty and the app falls back to import.meta.env / .env.* (see src/lib/config.ts).
window.__NATIVE_CONFIG__ = window.__NATIVE_CONFIG__ || {}
