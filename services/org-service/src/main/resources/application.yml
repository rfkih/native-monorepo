spring:
  application:
    name: org-service

  datasource:
    # Connect as the service's own NON-superuser role (no BYPASSRLS) so RLS is
    # genuinely enforced. Overridden per environment; in real deploys the creds
    # come from Vault (short-TTL dynamic credentials).
    url: ${DB_URL:jdbc:postgresql://localhost:5432/org_service}
    username: ${DB_USERNAME:org_service}
    password: ${DB_PASSWORD:org_service}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      # The schema is owned by Flyway; the app only validates that the mapping
      # matches the migrated schema and never mutates DDL.
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

management:
  endpoints:
    web:
      exposure:
        # Lean observability: liveness/readiness + Prometheus metrics. /healthz is
        # the simple LB probe (see HealthController); richer probes under /actuator.
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      # Pinned explicitly (never rely on Spring's implicit default): the unauthenticated
      # /actuator/health/** the gateway permits must NEVER leak DB/broker internals (version,
      # cluster-id, node-count, connection details). `never` keeps the body a bare {"status":...}.
      show-details: never
      # Readiness aggregates the DB indicator (ENGINEERING-STANDARDS §7): NOT_READY while
      # the schema/read model is hydrating or the DB is unreachable. org-service has no
      # Kafka consumer, so no `kafka` indicator here. Liveness stays the cheap /healthz probe.
      group:
        readiness:
          include: readinessState, db
  metrics:
    tags:
      service: org-service

# Structured JSON logs are defined ONCE in libs/observability's shared logback-native-json.xml
# (ENGINEERING-STANDARDS §5), included via this service's logback-spring.xml — dev keeps a console
# pattern, every non-dev profile emits one JSON object per line.

# Keycloak Admin REST client — active in ALL profiles (the signup endpoint uses it regardless of
# the JWT/dev security profile). The client secret defaults to the dev-realm value and is
# overridden in production via KEYCLOAK_ADMIN_SECRET (from Vault / env).
native:
  keycloak-admin:
    base-url: ${KEYCLOAK_BASE_URL:http://localhost:8080}
    realm: native
    client-id: native-admin
    client-secret: ${KEYCLOAK_ADMIN_SECRET:native-admin-secret}
    connect-timeout: 2s
    read-timeout: 5s
    # Signed-up owners get the VERIFY_EMAIL required action (blocks first login until the
    # address is verified). Default false: local/dev realms have no SMTP, and enabling it
    # without SMTP locks every new owner out at the verify screen. Production MUST set
    # KEYCLOAK_REQUIRE_EMAIL_VERIFICATION=true (with realm SMTP configured).
    require-email-verification: ${KEYCLOAK_REQUIRE_EMAIL_VERIFICATION:false}

---
# Defense-in-depth local JWT validation (#16, libs/security) — NON-DEV ONLY.
#
# This document activates on every profile EXCEPT `dev`. In a non-dev (production) profile the
# service validates the inbound RS256 token against Keycloak's JWKS locally (never trusting the
# gateway/header alone) and binds the tenant from the verified company_id claim; issuer-uri makes
# Spring validate the iss claim and discover the JWKS. Both default to the docker dev-stack
# Keycloak `native` realm and are overridden per environment via env/Vault.
#
# It is scoped to !dev on purpose: in the `dev` profile there is NO resource-server config, so
# Spring Boot's OAuth2 resource-server auto-configuration stays off and libs/security's permissive
# DevSecurityConfig governs while the header-trust DevTenantFilter binds the tenant (no Keycloak).
#
# NOTE org-service: POST /api/v1/companies bootstraps a new tenant — the owner creating their FIRST
# company has no company_id claim yet, because this request CREATES the tenant. It is still an
# authenticated endpoint (the JWT must be valid -> 401 if missing/invalid/expired), but a missing
# company_id claim must NOT be 403'd. We therefore declare it tenant-optional below
# (native.security.tenant-optional); libs/security's TenantBindingFilter then lets the tenant-less
# bootstrap token through WITHOUT binding a scope, and CompanyController opens its own scope over the
# new id. Every OTHER /api/v1/** request stays strictly tenant-scoped (tenant-less token -> 403).
# The DevTenantFilter (dev path only) documents the same bootstrap exemption via shouldNotFilter.
spring:
  config:
    activate:
      on-profile: "!dev"
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/native}
          jwk-set-uri: ${KEYCLOAK_JWK_SET_URI:http://localhost:8080/realms/native/protocol/openid-connect/certs}

native:
  security:
    # The tenant bootstrap: create-company creates a NEW tenant, so a valid token with no
    # company_id claim must reach it (no 403). Only this exact method+path is exempt — its
    # sub-resources (e.g. /api/v1/companies/{id}/businesses) remain strictly tenant-scoped.
    tenant-optional:
      - "POST /api/v1/companies"
    # The public sign-up endpoint: fully unauthenticated (no token at all). The security chain
    # permitAlls it; TenantBindingFilter skips it via shouldNotFilter. Default is empty — only
    # org-service opts in.
    public-paths:
      - "POST /api/v1/signup"
